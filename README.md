# password-generator
Client-side password generator with a cryptographically secure RNG, URL Hash API (for example, #16-az,AZ,09,json), dark theme, and UMD module. Works offline, with no dependencies. MIT.
